# Requests Route (`/requests`)

## Purpose
Real-time API traffic inspector and historical audit log explorer.

## Planned Features
* Live request/response stream with status code indicators
* Detailed inspect modal (Headers, Query Params, Body payloads, IP, User-Agent)
* Filter by status (Allowed, Blocked, Rate-Limited, Challenged)
* Search and export audit trails
